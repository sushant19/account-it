@model Banking.Domain.Operation
@using Banking.Web
    
<tr data-entity="operation" data-view="view" data-id=@Model.ID>
    <td>
        <input type="checkbox" class="itemCheck" data-action="select"/>
    </td>
    <td><span data-action="edit" title="Edit operation" data-sort-key="mark">@Model.Mark</span></td>
    <td><span data-action="edit" title="Edit operation" data-sort-key="amount">@Model.Amount.ToString(Helper.DecimalFormat)</span></td>
    <td><span data-action="edit" title="Edit operation" data-sort-key="description">@Model.Description</span></td>
    <td><span data-action="edit" title="Edit operation" data-sort-key="date" data-sort-value=@Model.Date.Ticks>@Model.Date.ToString(Helper.DateFormat)</span></td>
    <td>
        <div class="operationPersons" data-sort-key="participants" data-sort-value=@Model.Participants.Count>
            <ul>
                @foreach (var man in Model.Participants)
                {
                    <li>
                        <a href="/@man.Name" title="Go to @man.Name's page">@man.Name</a>
                        @if (Model.Participants.Last() != man) { Write(","); }
                    </li>
                }
            </ul>
        </div>
    </td>
</tr>      
    



